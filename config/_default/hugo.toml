baseURL = "https://zhangtianyuan120220.github.io/Sky_distant-s-Blog/"
languageCode = "zh-cn"
defaultContentLanguage = "zh"
title = "Sky_distant's Blog"
theme = "stack"

[params]
  [params.colorScheme]
    enabled = true
    default = "auto"

[params.sidebar]
  avatar = "/img/avatar.png"
  subtitle = "ACM / 算法竞赛笔记记录"

[outputs]
  home = [ "HTML", "JSON" ]   # 移除了 "RSS"，避免触发主题内部报错的 rss.xml
  page = [ "HTML" ]
  section = [ "HTML" ]